---
title: '两种预渲染方式'
date: '2020-01-01'
---
 
Next.js提供两种预渲染方式：**静态生成**和**服务器端渲染**。区别在于生成页面HTML的时机。
- **静态生成**是一种预渲染方法，可在**构建时**生成HTML。预渲染的HTML随后会在每次请求中被**重复使用**。
- **服务器端渲染**是一种预渲染方法，可在每次请求时生成HTML。
重要的是，Next.js允许你**自主选择**每个页面的预渲染形式。你可以通过为大多数页面使用静态生成，为其他页面使用服务器端渲染，从而创建一个“混合型”Next.js应用。
